public class MyApp extends PaperScreen {

  void setup() {
      setDrawingSize( 155, 108); // 297, 210);
    //    loadMarkerBoard(Papart.markerFolder + "big.cfg", 297, 210);
    // loadMarkerBoard(Papart.markerFolder + "A3-small1.cfg", 297, 210);

    loadMarkerBoard(Papart.markerFolder + "rocks.jpg", 155, 108);
    //loadMarkerBoard(Papart.markerFolder + "formeNaturelle.png", 140, 140);

    //    loadMarkerBoard(sketchPath() + "/data/formeNaturelle0.png", 140, 140);
    // loadMarkerBoard(sketchPath() + "/data/formeNaturelle.png", 297, 210);
    //     markerBoard.removeFiltering(cameraTracking);
  }

  void draw() {

      //        markerBoard.filter(cameraTracking);
      beginDraw2D();
    background(100, 0, 0, 130);
    fill(200, 100, 20); 
    rect(10, 10, 100, 30);
    endDraw();
  }
 }
